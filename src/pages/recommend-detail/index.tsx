import React from "react";
import { withRouter } from "react-router-dom";

const RecommendDetail = (props: any) => {
  console.log("props", props);
  return <div>推荐详情</div>;
};
export default withRouter(RecommendDetail);
